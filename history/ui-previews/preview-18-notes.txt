Preview 18 最小替换包

仅替换 AnimeRenamer.pyw。

改动：
1. 选中预览行后，DIARY LOG 显示完整源路径和目标路径。
2. DIARY LOG 改为可选中复制的只读输入栏，并增加 COPY 按钮复制完整源/目标路径。
3. 修复存在“待确认”时顶部状态被 update_run_state 覆盖为 PREVIEW SAFE 的问题。
4. “已跳过：关联视频已跳过”现在计入已跳过总数并使用跳过样式。
5. 状态摘要显示可执行、待确认/填写、冲突/错误、已跳过数量。
6. 执行确认框明确列出可执行、待确认和跳过数量。

测试：34/34 unittest 通过；定向状态/路径/剪贴板 smoke test 通过。
